(lambda (x y) (+ x y))

(define (add x y) (+ x y))
(define add 5)
add

(define add (lambda (x y) (+ x y)))
(add 1 1)

(define vhello "Hello World")
vhello
(define fhello (lambda () "Hello World"))
(fhello)
;diffrent each other